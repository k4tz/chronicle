# Chronicle client (Vite) environment.
# Only VITE_-prefixed vars are exposed to the browser bundle.

# Base URL of the Chronicle server API. Defaults to http://localhost:3001/api
# when unset (see src/api/client.ts).
VITE_API_URL="http://localhost:3001/api"
